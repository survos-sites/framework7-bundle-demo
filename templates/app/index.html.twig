{% extends 'base.html.twig' %}

{% block title %}Hello AppController!{% endblock %}

{% block content %}

    This is the INITIAL page, generated by PHP.
    the content is not (yet) loaded by the router, since it is the framework itself.

@no content while testing...
{% endblock %}

{% block body %}

    <div id="app">
        App info here.
    </div>
    {% set store='items' %}
    {% set globals  = {

    } %}
<twig:dexie
        refreshEvent="app.load"
        :store="store"
        :globals="globals"
        :filter="{}"
        :caller="_self">
    <twig:block name="title">
        there are {{ rows|length }} rows
    </twig:block>

    <twig:block name="twig_template" id="hack_to_get_raw_data">
        <span class="pokemon-count">there are {{ rows|length }} rows </span>
        <!-- hack_to_get_raw_data -->
    </twig:block>

</twig:dexie>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>/home/tac/g/sites/fw-bundle-demo/src/Controller/AppController.php</code></li>
        <li>Your template at <code>/home/tac/g/sites/fw-bundle-demo/templates/app/index.html.twig</code></li>
    </ul>
</div>
{% endblock %}
